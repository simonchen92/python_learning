from find_largest_num import find_max

numbers = [3, 6, 2, 8, 4, 10]
# max is also a built in function of python to find max number so we renaming it to maximum for practice purposes
maximum = find_max(numbers)
print(maximum)
